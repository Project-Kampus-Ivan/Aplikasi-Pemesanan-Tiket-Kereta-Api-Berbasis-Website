
package login;
import javax.swing.*;
public class FormLogin extends JFrame {
    public FormLogin(){
        setTitle("Login");
        setSize(400,250);
        setDefaultCloseOperation(EXIT_ON_CLOSE);
    }
}
